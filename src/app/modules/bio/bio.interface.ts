export type TSkills = {
    title: string;
    image: string;
}

export type TProjects = {
    image: string;
    title: string;
    description: string;
    features: string;
    category: [string];
    links: [string];
    technologies: [string];
} 